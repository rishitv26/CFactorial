/*
import system
using system.println;
using system.inputln;

void main -> (str[] args) {
	str name = inputln("> ");
    println(name);

	return exitcode(0);
}
*/
int x = 8;
int y = 9;
int main-> (int x);
